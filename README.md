# spam-ham
Within this project, I analyze word data to build a binary classifier (typically using Logistic Regression or Naive Bayes) that correctly separates junk mail (spam) from legitimate messages (ham).
