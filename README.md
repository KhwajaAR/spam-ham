# spam-ham
In this project, I analyzed word data to build a binary classifier (typically using Logistic Regression or Naive Bayes) that correctly separates junk mail (spam) from legitimate messages (ham).
